# Python 배경 제거 서버 실행 방법

## 방법 1: CMD (명령 프롬프트) 사용 - 가장 간단

1. **Win + R** 누르기
2. `cmd` 입력하고 엔터
3. 아래 명령어 복사해서 붙여넣기:

```cmd
cd C:\Users\KONG\Desktop\final\python-bg-remover
start.bat
```

---

## 방법 2: PowerShell 사용

PowerShell에서는 아래와 같이 실행하세요:

```powershell
cd C:\Users\KONG\Desktop\final\python-bg-remover
.\start.bat
```

또는 PowerShell 스크립트 실행:

```powershell
cd C:\Users\KONG\Desktop\final\python-bg-remover
.\start.ps1
```

**⚠️ 실행 정책 오류가 발생하면:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

## 방법 3: 파일 탐색기에서 더블클릭

1. 파일 탐색기 열기
2. `C:\Users\KONG\Desktop\final\python-bg-remover` 폴더로 이동
3. `start.bat` 파일을 **더블클릭**

---

## 방법 4: 수동 실행

```cmd
cd C:\Users\KONG\Desktop\final\python-bg-remover
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

---

## 서버가 정상 실행되면

다음과 같은 메시지가 나타납니다:

```
🚀 Background Removal Server Starting...
📍 Server running at: http://localhost:5000
🔍 Health check: http://localhost:5000/health
🎨 Remove BG endpoint: http://localhost:5000/remove-bg
 * Running on http://127.0.0.1:5000
```

브라우저에서 http://localhost:5000/health 접속하여 확인하세요.

---

## 문제 해결

### Python이 설치되지 않은 경우
https://www.python.org/downloads/ 에서 Python 3.8 이상 다운로드

### 포트 5000이 사용 중인 경우
`app.py` 마지막 줄을 수정:
```python
app.run(host='0.0.0.0', port=5001, debug=True)
```
